<!-- Navigation -->
<nav style="position: fixed; top: 0; left: 0; right: 0; background: rgba(10, 15, 30, 0.98); backdrop-filter: blur(20px); padding: 1.2rem 2.5rem; z-index: 50; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid rgba(168, 85, 247, 0.15); box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5); animation: fadeInDown 0.6s ease-out;">
  <div style="display: flex; justify-content: space-between; align-items: center; width: 100%; max-width: 1400px; margin: 0 auto;">
    <h1 style="font-size: 1.8rem; font-weight: 700; background: linear-gradient(135deg, #a855f7, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-transform: uppercase; letter-spacing: 2px; margin: 0;">
      <%= link_to "⚡ IENG", root_path, style: "text-decoration: none; color: inherit;" %>
    </h1>
    <div style="display: flex; gap: 2rem; align-items: center;">
      <%= link_to "Vehicles", vehicles_path, style: "text-decoration: none; color: #cbd5e1; transition: all 0.4s; font-weight: 500; text-transform: uppercase; letter-spacing: 1px; font-size: 0.9rem;" %>
      <%= link_to "Services", root_path, style: "text-decoration: none; color: #cbd5e1; transition: all 0.4s; font-weight: 500; text-transform: uppercase; letter-spacing: 1px; font-size: 0.9rem;" %>
      <% if current_user %>
        <%= link_to "Bookings", bookings_path, style: "text-decoration: none; color: #cbd5e1; transition: all 0.4s; font-weight: 500; text-transform: uppercase; letter-spacing: 1px; font-size: 0.9rem;" %>
        <div style="display: flex; align-items: center; gap: 1.5rem;">
          <span style="color: #cbd5e1; font-size: 0.9rem; font-weight: 500;"><%= current_user.email %></span>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, style: "text-decoration: none; padding: 0.75rem 1.5rem; background: linear-gradient(135deg, #ef4444, #dc2626); color: white; border-radius: 0.75rem; font-weight: 600; text-transform: uppercase; font-size: 0.85rem; letter-spacing: 1px; transition: all 0.3s;" %>
        </div>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, style: "text-decoration: none; padding: 0.75rem 1.5rem; background: linear-gradient(135deg, #a855f7, #ec4899); color: white; border-radius: 0.75rem; font-weight: 600; text-transform: uppercase; font-size: 0.85rem; letter-spacing: 1px; transition: all 0.3s;" %>
      <% end %>
    </div>
  </div>
</nav>

<style>
  @keyframes fadeInDown { from { opacity: 0; transform: translateY(-30px); } to { opacity: 1; transform: translateY(0); } }
</style>
