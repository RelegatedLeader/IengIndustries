<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ieng" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Ieng">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- GSAP Animation Library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
      /* Animations */
      @keyframes fadeInDown { from { opacity: 0; transform: translateY(-30px); } to { opacity: 1; transform: translateY(0); } }
      @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
      @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
      @keyframes slideInLeft { from { opacity: 0; transform: translateX(-40px); } to { opacity: 1; transform: translateX(0); } }
      @keyframes slideInRight { from { opacity: 0; transform: translateX(40px); } to { opacity: 1; transform: translateX(0); } }
      @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.8; } }
      @keyframes shimmer { 0% { background-position: -1000px 0; } 100% { background-position: 1000px 0; } }
      @keyframes glow { 0%, 100% { box-shadow: 0 0 20px rgba(168, 85, 247, 0.3); } 50% { box-shadow: 0 0 40px rgba(168, 85, 247, 0.6); } }
      @keyframes float { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-10px); } }
      
      * { margin: 0; padding: 0; box-sizing: border-box; }
      html, body { background: #000; color: #fff; font-family: 'Segoe UI', 'Helvetica Neue', sans-serif; line-height: 1.6; letter-spacing: 0.3px; }
      
      /* Navigation */
      nav { background: rgba(10, 15, 30, 0.98); backdrop-filter: blur(20px); padding: 1.2rem 2.5rem; position: sticky; top: 0; z-index: 50; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid rgba(168, 85, 247, 0.15); box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5); animation: fadeInDown 0.6s ease-out; }
      nav a { color: #cbd5e1; text-decoration: none; margin: 0 1.8rem; font-weight: 500; font-size: 0.95rem; transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); text-transform: uppercase; letter-spacing: 1px; }
      nav a:hover { color: #a855f7; transform: translateY(-2px); }
      .nav-brand { font-size: 1.8rem; font-weight: 700; background: linear-gradient(135deg, #a855f7, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-transform: uppercase; letter-spacing: 2px; animation: float 3s ease-in-out infinite; }
      
      /* Hero */
      .hero { min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg, #0a0f1e 0%, #1a1f3a 50%, #2d1b4e 100%); text-align: center; padding: 3rem 2rem; position: relative; overflow: hidden; }
      .hero::before { content: ''; position: absolute; top: -50%; right: -10%; width: 600px; height: 600px; background: radial-gradient(circle, rgba(168, 85, 247, 0.15) 0%, transparent 70%); animation: float 6s ease-in-out infinite; }
      .hero-content { position: relative; z-index: 1; animation: fadeInUp 0.8s ease-out; }
      .hero-content h1 { font-size: 4.5rem; font-weight: 700; background: linear-gradient(135deg, #a855f7, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin-bottom: 1.5rem; line-height: 1.1; letter-spacing: -2px; animation: fadeInUp 0.8s ease-out 0.2s both; }
      .hero-content p { font-size: 1.3rem; color: #94a3b8; margin-bottom: 3rem; max-width: 650px; margin-left: auto; margin-right: auto; animation: fadeInUp 0.8s ease-out 0.4s both; }
      .btn { padding: 1rem 2.5rem; border-radius: 0.75rem; font-weight: 700; cursor: pointer; border: none; transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); font-size: 1rem; text-transform: uppercase; letter-spacing: 1px; }
      .btn-primary { background: linear-gradient(135deg, #a855f7, #ec4899); color: #fff; box-shadow: 0 10px 30px rgba(168, 85, 247, 0.3); }
      .btn-primary:hover { transform: translateY(-4px); box-shadow: 0 25px 50px rgba(168, 85, 247, 0.5); }
      .btn-secondary { background: transparent; color: #a855f7; border: 2px solid #a855f7; }
      .btn-secondary:hover { background: rgba(168, 85, 247, 0.1); transform: translateY(-4px); }
      .button-group { display: flex; gap: 1.5rem; justify-content: center; animation: fadeInUp 0.8s ease-out 0.6s both; }
      
      /* Services Grid */
      .services { padding: 8rem 2rem; background: linear-gradient(180deg, #0a0f1e 0%, #000 100%); }
      .services h2 { text-align: center; font-size: 3rem; margin-bottom: 5rem; color: #fff; font-weight: 700; letter-spacing: -1px; animation: fadeInUp 0.8s ease-out; }
      .services-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2.5rem; max-width: 1400px; margin: 0 auto; }
      .service-card { background: rgba(20, 25, 50, 0.7); border: 1px solid rgba(168, 85, 247, 0.2); border-radius: 1.2rem; padding: 2.5rem; transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1); cursor: pointer; position: relative; overflow: hidden; animation: fadeInUp 0.8s ease-out; }
      .service-card::before { content: ''; position: absolute; top: 0; left: -100%; width: 100%; height: 100%; background: linear-gradient(90deg, transparent, rgba(168, 85, 247, 0.1), transparent); transition: left 0.6s ease; }
      .service-card:hover::before { left: 100%; }
      .service-card:hover { border-color: #a855f7; background: rgba(30, 50, 80, 0.8); transform: translateY(-8px); box-shadow: 0 25px 50px rgba(168, 85, 247, 0.2); }
      .service-icon { font-size: 3.5rem; margin-bottom: 1.5rem; display: inline-block; animation: float 3s ease-in-out infinite; }
      .service-card h3 { font-size: 1.5rem; margin: 1rem 0 0.75rem; color: #fff; font-weight: 700; }
      .service-card p { color: #94a3b8; margin-bottom: 1.5rem; line-height: 1.7; }
      .service-price { color: #ec4899; font-weight: 700; font-size: 1.3rem; }
      
      /* Features */
      .features { padding: 8rem 2rem; background: rgba(15, 23, 42, 0.4); }
      .features h2 { text-align: center; font-size: 3rem; margin-bottom: 5rem; color: #fff; font-weight: 700; animation: fadeInUp 0.8s ease-out; }
      .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 3rem; max-width: 1400px; margin: 0 auto; }
      .feature-item { text-align: center; padding: 2rem; border-radius: 1.2rem; background: rgba(30, 41, 59, 0.5); border: 1px solid rgba(168, 85, 247, 0.1); transition: all 0.5s ease; animation: fadeInUp 0.8s ease-out; }
      .feature-item:hover { border-color: #a855f7; transform: translateY(-5px); background: rgba(30, 50, 80, 0.7); }
      .feature-icon { font-size: 3rem; margin-bottom: 1.5rem; display: inline-block; animation: float 3s ease-in-out infinite; }
      .feature-item h3 { font-size: 1.5rem; margin-bottom: 1rem; color: #fff; font-weight: 700; }
      .feature-item p { color: #94a3b8; line-height: 1.7; }
      
      /* Footer */
      footer { background: #0a0f1e; border-top: 1px solid rgba(168, 85, 247, 0.1); padding: 5rem 2rem 2rem; }
      .footer-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 3rem; max-width: 1400px; margin: 0 auto; margin-bottom: 3rem; }
      .footer-section h4 { color: #fff; margin-bottom: 1.5rem; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; }
      .footer-section a { display: block; color: #94a3b8; text-decoration: none; margin: 0.75rem 0; transition: all 0.3s; }
      .footer-section a:hover { color: #a855f7; transform: translateX(5px); }
      .footer-bottom { text-align: center; color: #64748b; border-top: 1px solid rgba(168, 85, 247, 0.1); padding-top: 2rem; font-size: 0.9rem; }
      
      /* Responsive */
      @media (max-width: 768px) {
        .hero-content h1 { font-size: 2.5rem; }
        .services h2, .features h2 { font-size: 2rem; }
        .services-grid, .features-grid { grid-template-columns: 1fr; }
        .button-group { flex-direction: column; }
        nav { flex-direction: column; gap: 1rem; padding: 1rem; }
        nav a { margin: 0.5rem 0; }
        .hero { padding: 1.5rem; }
      }
    </style>
  </head>

  <body>
    <%= render "shared/navigation" %>
    <%= yield %>
  </body>
</html>
